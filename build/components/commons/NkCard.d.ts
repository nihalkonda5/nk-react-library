import { CardProps } from 'react-bootstrap';
export default function NkCard({ isLink, to, style, cardProps, children }: {
    isLink?: boolean;
    to?: string;
    style?: any;
    cardProps?: CardProps;
    children: any;
}): JSX.Element;
